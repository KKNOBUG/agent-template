fastapi==0.129.2
starlette==0.52.1            # 直接导入：request_context / HTTP 异常封装
uvicorn==0.49.0              # venv 另装有 [standard] 扩展：httptools/watchfiles/websockets
gunicorn==26.0.0             # 生产入口（Linux），UvicornWorker
sse-starlette==3.4.4         # SSE 流式响应
python-multipart==0.0.32     # FastAPI File/Form/UploadFile 运行期必需
aiofiles==25.1.0             # StaticFiles 静态资源挂载运行期必需
httpx==0.28.1                # embedding / rerank / VLM 的 HTTP 调用
pydantic==2.13.4
pydantic-core==2.46.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
typing-extensions==4.15.0
orjson==3.11.9
tortoise-orm==0.23.0
aerich==0.9.2                # 迁移管理（启动时自动 init）
asyncmy==0.2.11              # MySQL 异步驱动（tortoise.backends.mysql）
cryptography==48.0.1         # asyncmy 支持 MySQL 8 caching_sha2_password 认证所需
chromadb==1.5.9
pymilvus==3.0.0
milvus-lite==3.1.0           # Milvus 嵌入式模式
redis==8.0.0
celery==5.6.3
celery-redbeat==2.3.3        # beat 调度器：redbeat.schedulers:RedBeatScheduler
croniter==6.2.2
PyJWT==2.13.0                # JWT 签发/校验（services/dependency.py）
openai==2.48.0               # LLM / embedding / VLM 客户端
torch==2.12.0                # PyPI 默认构建（自带 CUDA 12.x），无需 PyTorch 专用索引
torchvision==0.27.0          # 与 torch 2.12 配对；docling-ibm-models 硬性依赖
numpy==2.4.6
docling==2.102.1             # 自动引入 docling-slim[standard]/ibm-models/docling-parse
docling-core==2.82.0         # 直接导入（docling_native.py）
rapidocr==3.8.3              # RapidOcrOptions OCR 引擎
onnxruntime==1.21.1          # RapidOCR 推理后端（恒 CPU）
pypdf==5.0.0                 # common/pdf_utils.py
loguru==0.7.3
concurrent-log-handler==0.9.29
portalocker==3.2.0           # 文件锁（services/persistence.py）
tenacity==9.1.4              # 重试
PyYAML==6.0.3                # yaml 配置读取